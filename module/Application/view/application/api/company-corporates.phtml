<?php if ($this->paginator->count()): ?>
<table class="table-data">
    <thead>
    <tr>
        <th class="text-left"><?= $this->translate('Contractor') ?></th>
        <th class="text-right" style="width:10rem"><?= $this->translate('Amount') ?></th>
    </tr>
    </thead>
    <tbody>
    <?php /** @var ArrayObject $row */
    $total = 0;
    foreach ($this->paginator as $row): ?>
        <tr>
            <td class="text-left hint"><?= $this->escapeHtml($row->offsetGet('contractor_name')) ?></td>
            <td class="text-right"><?= $this->currencyFormat($row->offsetGet('amount')) ?></td>
        </tr>
        <?php $total += $row->offsetGet('amount') ?>
    <?php endforeach; ?>
    <caption><?= sprintf('%s: %s',
            $this->translate('The total amount of charter capital'),
            $this->currencyFormat($total)) ?></caption>
    </tbody>
</table>
<?php else: ?>
    <div class="text-center">
        <img src="<?= $this->basePath('img/empty.svg') ?>" alt="Empty folder" style="max-width:200px">
        <h3><?= $this->translate('Nothing found') ?></h3>
    </div>
<?php endif ?>